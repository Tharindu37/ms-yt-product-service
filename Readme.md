### Start Project
1. Click the Maven icon
2. Click the Execute Maven Goal icon
3. Run the command
```aiignore
mvn clean verify
```

### docker-compose.yml
```aiignore
docker compose up -d
```